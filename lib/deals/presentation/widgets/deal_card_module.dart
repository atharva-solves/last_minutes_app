import 'package:flutter/material.dart';

class DealCardModule extends StatelessWidget {
  const DealCardModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}